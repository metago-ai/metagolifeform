"use strict";
/**
 * MetaGO Engine - T3 概念存档 · 冲突族（X 族）
 *
 * ALG_T3_X_001 ~ ALG_T3_X_014，共 14 个概念。
 * 覆盖冲突识别、分析、转化、调解、仲裁、升级、降级、预防、溯源、记忆、进化、利益冲突、价值冲突、资源冲突。
 *
 * @author 易霄 / MetaGO Lightyear
 * @version 1.0.0
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.CONCEPTS = void 0;
exports.getConceptById = getConceptById;
exports.searchConcepts = searchConcepts;
exports.CONCEPTS = [
    {
        id: 'ALG_T3_X_001',
        name: '冲突识别方法',
        family: 'CONFLICT',
        category: '冲突识别',
        complexity: 'O(n²)',
        description: '冲突识别方法检测系统中存在的冲突——目标冲突、资源冲突、价值冲突、利益冲突。识别基于矛盾检测——两个目标无法同时满足、两个需求竞争同一资源、两个价值取向对立。识别支持显式冲突（明确表达的矛盾）和隐式冲突（行为表现的矛盾）。冲突识别是冲突管理的第一步——只有识别冲突才能处理冲突。',
        inputSchema: '{ entities: { id: string; goals: string[]; values: string[]; resources: string[] }[]; interactions?: { a: string; b: string; type: string }[] }',
        outputSchema: '{ conflicts: { type: "goal" | "resource" | "value" | "interest"; parties: string[]; description: string; severity: "low" | "medium" | "high" }[]; conflictCount: number; criticalConflicts: number }',
        keywords: ['冲突', '识别', '矛盾', '目标', '资源', '价值'],
        relatedAlgorithms: ['ALG_T3_V_003', 'ALG_T3_X_002', 'ALG_T3_L_009'],
        documentationRef: '附录A·T3·冲突·冲突识别方法',
        useCases: ['冲突管理', '矛盾检测', '风险识别', '系统诊断'],
        limitations: ['隐式冲突难识别', '需多方信息', '可能误判'],
        provenance: ['元构43条根本属性·D39直接批判性', '冲突识别理论'],
    },
    {
        id: 'ALG_T3_X_002',
        name: '冲突分析方法',
        family: 'CONFLICT',
        category: '冲突分析',
        complexity: 'O(n·m)',
        description: '冲突分析方法深入分析已识别冲突的性质、原因、影响。分析维度：冲突根源（根本原因）、冲突强度（激烈程度）、冲突范围（影响范围）、冲突历史（演化过程）、冲突趋势（发展方向）。分析支持冲突地图——可视化冲突各方的关系和立场。分析结果指导冲突处理策略选择——不同冲突需不同处理方式。',
        inputSchema: '{ conflict: { type: string; parties: string[]; description: string }; context: { history: Event[]; resources: Resource[]; stakeholders: string[] } }',
        outputSchema: '{ analysis: { rootCause: string; intensity: number; scope: string[]; history: string; trend: "escalating" | "stable" | "de-escalating"; stakeholders: { party: string; position: string; interest: string; power: number }[] }; conflictMap: ConflictMap }',
        keywords: ['分析', '根源', '强度', '范围', '趋势'],
        relatedAlgorithms: ['ALG_T3_X_001', 'ALG_T3_B_013', 'ALG_T3_R_005'],
        documentationRef: '附录A·T3·冲突·冲突分析方法',
        useCases: ['冲突诊断', '处理策略', '风险评估', '调解准备'],
        limitations: ['信息可能不全', '各方立场主观', '动态变化'],
        provenance: ['元构43条根本属性·D40', '冲突分析理论'],
    },
    {
        id: 'ALG_T3_X_003',
        name: '冲突转化策略',
        family: 'CONFLICT',
        category: '冲突转化',
        complexity: 'O(n·m)',
        description: '冲突转化策略将破坏性冲突转化为建设性创新。转化基于重构——重新定义问题，使冲突变为创新契机。转化支持：竞争转合作（零和博弈转正和博弈）、分歧转多元（对立转互补）、危机转机遇（威胁转机会）。转化的关键是视角转换——从对立视角转向共赢视角。冲突转化是冲突管理的最高境界。',
        inputSchema: '{ conflict: { type: string; parties: string[]; positions: { party: string; wants: string }[] }; transformationGoal?: string }',
        outputSchema: '{ transformed: boolean; newFraming: string; winWinSolution: { party: string; gains: string; concessions: string }[]; innovationOpportunity: string; sustainability: number }',
        keywords: ['转化', '重构', '共赢', '创新契机'],
        relatedAlgorithms: ['ALG_T3_X_004', 'ALG_T3_M_010', 'ALG_T3_D_009'],
        documentationRef: '附录A·T3·冲突·冲突转化策略',
        useCases: ['冲突创新', '共赢方案', '危机转机遇', '关系修复'],
        limitations: ['转化需双方意愿', '需创造性思维', '可能失败'],
        provenance: ['元构核心公理·A35', '冲突转化理论'],
    },
    {
        id: 'ALG_T3_X_004',
        name: '冲突调解方法',
        family: 'CONFLICT',
        category: '冲突调解',
        complexity: 'O(n·m)',
        description: '冲突调解方法由第三方调解人协助冲突各方达成协议。调解基于中立性——调解人不偏袒任何一方。调解过程：建立信任、明确立场、挖掘利益、生成方案、达成协议。调解支持促成式调解（帮助各方自己解决）和评估式调解（调解人提建议）。调解的关键是利益而非立场——立场对立但利益可能兼容。',
        inputSchema: '{ parties: { id: string; position: string; interests: string[]; constraints: string[] }[]; mediator?: { style: "facilitative" | "evaluative" | "transformative" } }',
        outputSchema: '{ agreement: { terms: string; party: string; obligations: string[]; benefits: string[] }[]; satisfaction: Record<string, number>; durability: number; implementationPlan: string }',
        keywords: ['调解', '中立', '利益', '协议'],
        relatedAlgorithms: ['ALG_T3_X_003', 'ALG_T3_X_005', 'ALG_T3_D_010'],
        documentationRef: '附录A·T3·冲突·冲突调解方法',
        useCases: ['纠纷解决', '关系修复', '协议达成', '团队协调'],
        limitations: ['需双方意愿', '调解人能力影响', '可能失败'],
        provenance: ['元构43条根本属性·D38', '冲突调解理论'],
    },
    {
        id: 'ALG_T3_X_005',
        name: '冲突仲裁机制',
        family: 'CONFLICT',
        category: '冲突仲裁',
        complexity: 'O(n)',
        description: '冲突仲裁机制在调解失败时，由仲裁人做出有约束力的裁决。仲裁基于规则和证据——仲裁人依据既定规则和双方证据裁决。仲裁分约束性仲裁（裁决有法律效力）和非约束性仲裁（裁决是建议）。仲裁的关键是公正性——仲裁人必须公正、独立、专业。仲裁是冲突解决的最后手段，适用于调解无效的场景。',
        inputSchema: '{ parties: { id: string; claim: string; evidence: string[] }[]; arbiter: { qualifications: string[]; impartiality: number }; rules: string[] }',
        outputSchema: '{ ruling: { decision: string; rationale: string; obligations: { party: string; obligation: string; deadline: number }[]; binding: boolean }; fairnessScore: number; appealable: boolean }',
        keywords: ['仲裁', '裁决', '公正', '约束力'],
        relatedAlgorithms: ['ALG_T3_X_004', 'ALG_T3_S_002', 'ALG_T3_D_007'],
        documentationRef: '附录A·T3·冲突·冲突仲裁机制',
        useCases: ['纠纷裁决', '合同争议', '强制解决', '法律替代'],
        limitations: ['需双方同意仲裁', '可能不公', '可能被上诉'],
        provenance: ['元构43条根本属性·D38', '冲突仲裁理论'],
    },
    {
        id: 'ALG_T3_X_006',
        name: '冲突升级机制',
        family: 'CONFLICT',
        category: '冲突升级',
        complexity: 'O(n)',
        description: '冲突升级机制描述冲突从低强度向高强度发展的过程。升级阶段：潜在冲突（未爆发）→显现冲突（开始对抗）→激烈冲突（全面对抗）→暴力冲突（破坏性）。升级的驱动因素：误解加深、信任破裂、利益受损、情绪激化。升级机制识别升级信号——早期识别可阻止升级。升级管理是冲突预防的核心。',
        inputSchema: '{ conflict: { currentStage: string; history: { stage: string; time: number; trigger: string }[] }; escalationFactors: string[] }',
        outputSchema: '{ escalationRisk: number; currentStage: string; nextStage: string; warningSignals: string[]; interventionWindow: number; deEscalationActions: string[] }',
        keywords: ['升级', '阶段', '信号', '干预'],
        relatedAlgorithms: ['ALG_T3_X_007', 'ALG_T3_B_015', 'ALG_T3_X_008'],
        documentationRef: '附录A·T3·冲突·冲突升级机制',
        useCases: ['冲突预防', '早期预警', '干预时机', '升级阻止'],
        limitations: ['升级难预测', '需持续监测', '外部因素影响'],
        provenance: ['元构43条根本属性·D39', '冲突升级理论'],
    },
    {
        id: 'ALG_T3_X_007',
        name: '冲突降级策略',
        family: 'CONFLICT',
        category: '冲突降级',
        complexity: 'O(n·m)',
        description: '冲突降级策略将高强度冲突降为低强度冲突。降级方法：冷却（暂停对抗，冷静情绪）、沟通（恢复对话，消除误解）、让步（适度让步，释放善意）、第三方介入（引入中立第三方）。降级的关键是打破对抗循环——一方先释放善意，另一方回应。降级是冲突转化的前置——先降级才能转化。',
        inputSchema: '{ conflict: { currentStage: string; intensity: number; parties: string[] }; strategy?: "cooling" | "communication" | "concession" | "intervention" }',
        outputSchema: '{ deEscalated: boolean; newStage: string; newIntensity: number; actionsTaken: { action: string; effect: string; time: number }[]; stability: number }',
        keywords: ['降级', '冷却', '沟通', '让步'],
        relatedAlgorithms: ['ALG_T3_X_006', 'ALG_T3_X_003', 'ALG_T3_X_004'],
        documentationRef: '附录A·T3·冲突·冲突降级策略',
        useCases: ['冲突缓和', '对抗降低', '关系修复', '转化前置'],
        limitations: ['需双方配合', '可能反复', '需持续努力'],
        provenance: ['元构43条根本属性·D39', '冲突降级理论'],
    },
    {
        id: 'ALG_T3_X_008',
        name: '冲突预防机制',
        family: 'CONFLICT',
        category: '冲突预防',
        complexity: 'O(n)',
        description: '冲突预防机制在冲突爆发前消除冲突根源。预防基于冲突预警——识别潜在冲突的早期信号。预防措施：沟通机制（定期沟通消除误解）、利益对齐（对齐各方利益）、规则明确（明确规则减少争议）、缓冲设计（设计缓冲减少摩擦）。预防优于治疗——预防成本远低于处理成本。预防是冲突管理的最高境界。',
        inputSchema: '{ system: { entities: Entity[]; interactions: Interaction[]; history: Conflict[] }; riskFactors: string[] }',
        outputSchema: '{ preventionRisk: number; potentialConflicts: { type: string; parties: string[]; probability: number; preventionMeasures: string[] }[]; recommendedActions: string[] }',
        keywords: ['预防', '预警', '沟通', '对齐', '缓冲'],
        relatedAlgorithms: ['ALG_T3_X_006', 'ALG_T3_B_015', 'ALG_T3_X_001'],
        documentationRef: '附录A·T3·冲突·冲突预防机制',
        useCases: ['冲突预防', '风险消除', '关系维护', '系统设计'],
        limitations: ['不能完全预防', '需持续投入', '需预警能力'],
        provenance: ['元构43条根本属性·D39', '冲突预防理论'],
    },
    {
        id: 'ALG_T3_X_009',
        name: '冲突溯源方法',
        family: 'CONFLICT',
        category: '冲突溯源',
        complexity: 'O(n)',
        description: '冲突溯源方法追溯冲突的根本原因——从表面冲突回溯到深层根源。溯源基于因果链——冲突的触发事件→直接原因→深层原因→根本原因。常见根本原因：资源稀缺、价值差异、利益冲突、沟通缺失、历史遗留。溯源支持冲突归因（谁的责任）和冲突预防（消除根源防复发）。溯源是彻底解决冲突的基础。',
        inputSchema: '{ conflict: { triggerEvent: string; immediateCause: string; parties: string[] }; history: { time: number; event: string; actor: string }[] }',
        outputSchema: '{ rootCause: string; causalChain: { level: number; cause: string; evidence: string }[]; attribution: { party: string; responsibility: number }[]; preventionPoints: string[] }',
        keywords: ['溯源', '根本原因', '因果链', '归因'],
        relatedAlgorithms: ['ALG_T3_B_013', 'ALG_T3_R_005', 'ALG_T3_V_009'],
        documentationRef: '附录A·T3·冲突·冲突溯源方法',
        useCases: ['根因分析', '责任划分', '预防复发', '彻底解决'],
        limitations: ['因果链可能复杂', '归因主观', '历史信息缺失'],
        provenance: ['元构核心公理·A1溯源公理', '冲突溯源理论'],
    },
    {
        id: 'ALG_T3_X_010',
        name: '冲突记忆机制',
        family: 'CONFLICT',
        category: '冲突记忆',
        complexity: 'O(n)',
        description: '冲突记忆机制记录历史冲突及其解决过程，形成可复用的冲突处理经验。记忆分三类：成功解决（什么方法有效）、失败解决（什么方法无效）、冲突模式（什么类型冲突如何处理）。记忆支持检索（按冲突类型找类似案例）和迁移（将解决方法应用到新冲突）。冲突记忆使冲突处理能力可积累、可复用、可进化。',
        inputSchema: '{ conflict: { type: string; resolution: string; outcome: "success" | "failure" }; lessons?: string[] }',
        outputSchema: '{ stored: boolean; memoryType: "success" | "failure" | "pattern"; retrievalKeys: string[]; transferableTo: string[]; similarPastConflicts: { conflict: string; resolution: string; similarity: number }[] }',
        keywords: ['记忆', '经验', '复用', '迁移'],
        relatedAlgorithms: ['ALG_T3_K_001', 'ALG_T3_D_012', 'ALG_T3_M_014'],
        documentationRef: '附录A·T3·冲突·冲突记忆机制',
        useCases: ['冲突处理经验', '模式复用', '能力积累', '决策支持'],
        limitations: ['记忆可能过时', '场景差异', '需适配'],
        provenance: ['元构43条根本属性·D43', '冲突记忆理论'],
    },
    {
        id: 'ALG_T3_X_011',
        name: '冲突进化机制',
        family: 'CONFLICT',
        category: '冲突进化',
        complexity: 'O(n·m)',
        description: '冲突进化机制将冲突处理能力本身作为进化对象——从每次冲突处理中学习，改进处理方法。进化基于反馈循环——冲突→处理→结果→反馈→改进→再冲突。进化支持元冲突处理——处理"如何更好处理冲突"的方法。冲突进化使处理能力持续提升，从被动应对到主动预防到冲突转化。是冲突管理的最高形态。',
        inputSchema: '{ history: { conflict: string; method: string; outcome: string; feedback: string; lesson: string }[]; currentCapability: number }',
        outputSchema: '{ evolvedCapability: number; evolutionStage: "reactive" | "proactive" | "transformative"; accuracy: number; preventionRate: number; nextEvolutionFocus: string }',
        keywords: ['进化', '反馈', '元冲突', '学习'],
        relatedAlgorithms: ['ALG_T3_E_006', 'ALG_T3_X_010', 'ALG_T3_D_013'],
        documentationRef: '附录A·T3·冲突·冲突进化机制',
        useCases: ['处理能力提升', '持续改进', '冲突预防', '冲突转化'],
        limitations: ['进化缓慢', '需充分反馈', '可能退化'],
        provenance: ['元构核心公理·A3', '冲突进化理论'],
    },
    {
        id: 'ALG_T3_X_012',
        name: '利益冲突管理',
        family: 'CONFLICT',
        category: '利益冲突',
        complexity: 'O(n²)',
        description: '利益冲突管理专门处理各方利益对立的冲突。利益冲突是零和博弈——一方所得是另一方所失。管理方法：利益重组（重新定义利益，使零和转正和）、利益补偿（受益方补偿受损方）、利益优先级（按优先级排序，高优先级优先）、利益分离（将冲突利益分离到不同领域）。利益冲突是最常见的冲突类型。',
        inputSchema: '{ parties: { id: string; interests: { name: string; importance: number; flexibility: number }[] }[]; zeroSumResources?: string[] }',
        outputSchema: '{ management: { strategy: "restructure" | "compensate" | "prioritize" | "separate"; details: string; party: string; gain: string; sacrifice: string }[]; zeroSumResolved: boolean; totalGain: number; fairness: number }',
        keywords: ['利益', '零和', '正和', '补偿', '优先级'],
        relatedAlgorithms: ['ALG_T3_X_001', 'ALG_T3_X_003', 'ALG_T3_D_009'],
        documentationRef: '附录A·T3·冲突·利益冲突管理',
        useCases: ['利益协调', '零和转化', '资源分配', '合作设计'],
        limitations: ['并非所有零和可转化', '补偿需资源', '优先级主观'],
        provenance: ['元构43条根本属性·D38', '利益冲突理论'],
    },
    {
        id: 'ALG_T3_X_013',
        name: '价值冲突处理',
        family: 'CONFLICT',
        category: '价值冲突',
        complexity: 'O(n·m)',
        description: '价值冲突处理专门处理各方价值取向对立的冲突。价值冲突比利益冲突更难——价值是核心信念，难以妥协。处理方法：尊重差异（承认价值多元）、寻找共性（寻找共同价值）、分级处理（核心价值不可妥协，外围价值可协商）、价值升华（提升到更高价值统合）。价值冲突需长期培育——通过教育和交流逐步化解。',
        inputSchema: '{ parties: { id: string; values: { name: string; importance: number; negotiable: boolean }[] }[]; commonGround?: string[] }',
        outputSchema: '{ resolution: { method: "respect" | "common-ground" | "tiered" | "elevate"; details: string; commonValues: string[]; irreconcilable: string[] }; relationshipImpact: number; sustainability: number }',
        keywords: ['价值', '多元', '共性', '升华'],
        relatedAlgorithms: ['ALG_T3_V_003', 'ALG_T3_X_003', 'ALG_T3_V_002'],
        documentationRef: '附录A·T3·冲突·价值冲突处理',
        useCases: ['文化冲突', '意识形态对立', '组织融合', '价值对齐'],
        limitations: ['核心价值难妥协', '需长期培育', '可能无法解决'],
        provenance: ['元构29维价值对齐', '价值冲突理论'],
    },
    {
        id: 'ALG_T3_X_014',
        name: '资源冲突解决',
        family: 'CONFLICT',
        category: '资源冲突',
        complexity: 'O(n log n)',
        description: '资源冲突解决处理各方竞争同一稀缺资源的冲突。解决方法：资源增加（增加资源供给）、资源分配（优化分配方案）、资源共享（共享使用）、资源替代（寻找替代资源）、资源节约（减少需求）。解决的关键是帕累托改进——使至少一方更好而没有任何一方更差。资源冲突是最具体的冲突，相对容易解决。',
        inputSchema: '{ resource: { type: string; total: number; scarcity: number }; parties: { id: string; demand: number; urgency: number; alternative?: string }[] }',
        outputSchema: '{ solution: { method: "increase" | "allocate" | "share" | "substitute" | "conserve"; details: string }; allocation: { party: string; amount: number; satisfaction: number }[]; paretoOptimal: boolean; efficiency: number; fairness: number }',
        keywords: ['资源', '稀缺', '分配', '帕累托', '共享'],
        relatedAlgorithms: ['ALG_T3_X_012', 'ALG_T3_D_009', 'ALG_T3_D_010'],
        documentationRef: '附录A·T3·冲突·资源冲突解决',
        useCases: ['资源分配', '稀缺管理', '共享设计', '效率优化'],
        limitations: ['资源可能无法增加', '帕累托不一定存在', '需妥协'],
        provenance: ['元构43条根本属性·D38', '资源冲突理论'],
    },
];
function getConceptById(id) {
    return exports.CONCEPTS.find(c => c.id === id);
}
function searchConcepts(keywords) {
    const lowerKeywords = keywords.map(k => k.toLowerCase());
    return exports.CONCEPTS.filter(c => c.keywords.some(k => lowerKeywords.some(lk => k.toLowerCase().includes(lk))) ||
        lowerKeywords.some(lk => c.name.toLowerCase().includes(lk) || c.description.toLowerCase().includes(lk)));
}
