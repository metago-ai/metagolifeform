"use strict";
/**
 * MetaGO Engine - T3 概念存档 · 时空族（T 族）
 *
 * ALG_T3_T_001 ~ ALG_T3_T_014，共 14 个概念。
 * 覆盖时间衰减、序列、窗口、戳、同步、漂移、预算、优化、感知、记忆、推理、冲突、平衡、进化。
 *
 * @author 易霄 / MetaGO Lightyear
 * @version 1.0.0
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.CONCEPTS = void 0;
exports.getConceptById = getConceptById;
exports.searchConcepts = searchConcepts;
exports.CONCEPTS = [
    {
        id: 'ALG_T3_T_001',
        name: '时间衰减模型',
        family: 'TIME',
        category: '时间衰减',
        complexity: 'O(1)',
        description: '时间衰减模型描述事物重要性随时间推移的衰减规律。基于半衰期理论，重要性按指数衰减——每过一个半衰期，重要性减半。衰减模型支持：当前重要性计算（基于初始重要性和经过时间）、半衰期设定（不同类型不同半衰期）、衰减补偿（通过维护行为延缓衰减）。时间衰减是耦生衰减、价值衰减、记忆衰减的共同基础模型。',
        inputSchema: '{ initialValue: number; halfLife: number; elapsed: number; maintenanceEvents?: { time: number; boost: number }[] }',
        outputSchema: '{ currentValue: number; decayRate: number; remainingHalfLives: number; nextMaintenanceDue: number; decayPattern: "exponential" | "linear" | "logarithmic" }',
        keywords: ['衰减', '半衰期', '指数', '维护'],
        relatedAlgorithms: ['ALG_T1_C_014', 'ALG_T3_C_005', 'ALG_T3_V_004'],
        documentationRef: '附录A·T3·时空·时间衰减模型',
        useCases: ['重要性评估', '维护时机', '衰减预测', '优先级排序'],
        limitations: ['模型理想化', '类型差异大', '外部事件影响'],
        provenance: ['元构43条根本属性·D41', '时间衰减理论'],
    },
    {
        id: 'ALG_T3_T_002',
        name: '时间序列分析',
        family: 'TIME',
        category: '时间序列',
        complexity: 'O(n log n)',
        description: '时间序列分析处理按时间顺序排列的数据序列。分析方法包括：趋势分析（识别长期趋势）、季节性分析（识别周期性）、异常检测（识别异常点）、预测（外推未来值）。分析支持自回归（AR）、移动平均（MA）、ARIMA、LSTM 等模型。时间序列分析是预测的基础——从历史预测未来。',
        inputSchema: '{ series: { time: number; value: number }[]; methods?: string[]; forecastHorizon?: number }',
        outputSchema: '{ trend: { direction: string; slope: number; confidence: number }; seasonality: { period: number; amplitude: number }; anomalies: { time: number; value: number; severity: string }[]; forecast: { time: number; value: number; confidence: number }[]; modelAccuracy: number }',
        keywords: ['时间序列', '趋势', '季节性', '预测', 'ARIMA'],
        relatedAlgorithms: ['ALG_T1_C_024', 'ALG_T3_F_013', 'ALG_T3_B_005'],
        documentationRef: '附录A·T3·时空·时间序列分析',
        useCases: ['趋势预测', '异常检测', '容量规划', '决策支持'],
        limitations: ['需历史数据', '假设历史延续', '黑天鹅不可预测'],
        provenance: ['元构43条根本属性·D40', '时间序列理论'],
    },
    {
        id: 'ALG_T3_T_003',
        name: '时间窗口机制',
        family: 'TIME',
        category: '时间窗口',
        complexity: 'O(n)',
        description: '时间窗口机制将数据按时间窗口分组处理。窗口类型：滚动窗口（固定大小滑动）、跳跃窗口（固定间隔跳跃）、会话窗口（基于活跃间隔）、全局窗口（全部数据）。窗口支持聚合（窗口内数据聚合）和关联（跨窗口数据关联）。窗口机制是流处理的基础——将无限数据流切分为有限块处理。',
        inputSchema: '{ stream: { time: number; data: unknown }[]; windowType?: "tumbling" | "sliding" | "session" | "global"; size?: number; slide?: number }',
        outputSchema: '{ windows: { start: number; end: number; count: number; aggregated: unknown }[]; windowCount: number; coverage: number; lateness: number }',
        keywords: ['窗口', '滚动', '跳跃', '会话', '流处理'],
        relatedAlgorithms: ['ALG_T3_T_002', 'ALG_T3_G_008', 'ALG_T3_B_006'],
        documentationRef: '附录A·T3·时空·时间窗口机制',
        useCases: ['流处理', '实时聚合', '窗口计算', '数据切分'],
        limitations: ['窗口大小选择', '延迟数据处理', '状态管理复杂'],
        provenance: ['元构43条根本属性·D40', '时间窗口理论'],
    },
    {
        id: 'ALG_T3_T_004',
        name: '时间戳管理',
        family: 'TIME',
        category: '时间戳',
        complexity: 'O(1)',
        description: '时间戳管理为每个事件附加精确的时间标记。时间戳支持：创建时间（事件创建时）、修改时间（事件修改时）、过期时间（事件失效时）、版本时间（版本控制）。管理解决时间精度问题——毫秒级、微秒级、纳秒级。管理支持逻辑时钟（Lamport 时间戳）和物理时钟（NTP 同步）。时间戳是事件排序和因果推理的基础。',
        inputSchema: '{ events: { id: string; data: unknown; timestamp?: number }[]; clockType?: "physical" | "logical" | "hybrid" }',
        outputSchema: '{ timestamped: { id: string; data: unknown; createdAt: number; version: number }[]; ordering: string[]; conflicts: { events: string[]; reason: string }[] }',
        keywords: ['时间戳', '版本', '逻辑时钟', '排序'],
        relatedAlgorithms: ['ALG_T3_T_005', 'ALG_T3_K_009', 'ALG_T3_R_005'],
        documentationRef: '附录A·T3·时空·时间戳管理',
        useCases: ['事件排序', '版本控制', '因果推理', '审计追踪'],
        limitations: ['时钟同步问题', '精度有限', '分布式复杂'],
        provenance: ['元构核心公理·A1溯源公理', '时间戳理论'],
    },
    {
        id: 'ALG_T3_T_005',
        name: '时间同步方法',
        family: 'TIME',
        category: '时间同步',
        complexity: 'O(n)',
        description: '时间同步方法确保分布式系统中各节点的时间一致。方法包括 NTP（网络时间协议）、PTP（精确时间协议）、逻辑时钟（Lamport）、向量时钟。同步解决时钟漂移——各节点时钟速率不同导致时间偏差。同步精度从毫秒级（NTP）到纳秒级（PTP）不等。时间同步是分布式系统一致性的基础。',
        inputSchema: '{ nodes: { id: string; localTime: number; driftRate?: number }[]; protocol?: "ntp" | "ptp" | "lamport" | "vector"; precision?: number }',
        outputSchema: '{ synchronized: boolean; referenceTime: number; offsets: { node: string; offset: number; residual: number }[]; precision: number; driftCompensation: number }',
        keywords: ['同步', 'NTP', 'PTP', '逻辑时钟', '漂移'],
        relatedAlgorithms: ['ALG_T3_T_004', 'ALG_T3_T_006', 'ALG_T3_K_008'],
        documentationRef: '附录A·T3·时空·时间同步方法',
        useCases: ['分布式系统', '事件排序', '一致性保证', '日志关联'],
        limitations: ['网络延迟影响', '不可能完全同步', '需持续同步'],
        provenance: ['元构43条根本属性·D40', '时间同步理论'],
    },
    {
        id: 'ALG_T3_T_006',
        name: '时间漂移检测',
        family: 'TIME',
        category: '时间漂移',
        complexity: 'O(n)',
        description: '时间漂移检测识别系统时间与参考时间的偏差。漂移源于时钟速率差异——石英晶体的频率不精确导致累积偏差。检测基于参考时间比对——将本地时间与 NTP 服务器时间对比。漂移分线性漂移（固定速率偏差）和非线性漂移（速率变化）。检测结果用于时钟校准——补偿漂移使本地时间保持准确。',
        inputSchema: '{ localTime: number; referenceTime: number; history?: { local: number; reference: number; time: number }[] }',
        outputSchema: '{ drift: number; driftRate: number; driftType: "linear" | "nonlinear"; correctionNeeded: boolean; correctionAmount: number; confidence: number }',
        keywords: ['漂移', '时钟', '偏差', '校准'],
        relatedAlgorithms: ['ALG_T3_B_001', 'ALG_T3_T_005', 'ALG_T3_B_007'],
        documentationRef: '附录A·T3·时空·时间漂移检测',
        useCases: ['时钟校准', '时间准确性', '分布式同步', '日志关联'],
        limitations: ['需参考时间', '网络延迟影响', '检测间隔影响'],
        provenance: ['元构43条根本属性·D38', '时间漂移理论'],
    },
    {
        id: 'ALG_T3_T_007',
        name: '时间预算管理',
        family: 'TIME',
        category: '时间预算',
        complexity: 'O(n log n)',
        description: '时间预算管理为任务分配时间资源并追踪使用情况。预算包括：总预算（任务总时间）、阶段预算（各阶段时间）、缓冲预算（应对意外）。管理支持预算分配（如何分配）、预算追踪（已用多少）、预算预警（即将超支）、预算调整（必要时调整）。时间预算是项目管理和个人效率的核心工具。',
        inputSchema: '{ task: { name: string; totalBudget: number; phases: { name: string; budget: number; buffer?: number }[] }; tracking?: { phase: string; used: number }[] }',
        outputSchema: '{ allocation: { phase: string; budget: number; buffer: number; allocated: number }[]; usage: { phase: string; used: number; remaining: number; utilization: number; status: "on-track" | "warning" | "over" }[]; totalUsed: number; totalRemaining: number; alerts: string[] }',
        keywords: ['预算', '分配', '追踪', '预警', '缓冲'],
        relatedAlgorithms: ['ALG_T3_D_008', 'ALG_T3_D_010', 'ALG_T1_V_017'],
        documentationRef: '附录A·T3·时空·时间预算管理',
        useCases: ['项目管理', '时间管理', '效率优化', '资源规划'],
        limitations: ['预算估算不准', '外部干扰', '需持续追踪'],
        provenance: ['元构43条根本属性·D38', '时间预算理论'],
    },
    {
        id: 'ALG_T3_T_008',
        name: '时间优化策略',
        family: 'TIME',
        category: '时间优化',
        complexity: 'O(n log n)',
        description: '时间优化策略通过减少时间消耗提升效率。优化方向：并行化（串行转并行）、消除等待（减少空闲）、批处理（小任务合并）、缓存（避免重复计算）、预计算（提前计算）。优化支持关键路径分析——识别最耗时的关键路径，优先优化。优化是性能工程的核心——在有限时间内完成更多工作。',
        inputSchema: '{ process: { steps: { name: string; duration: number; dependencies: number[] }[] }; constraints?: { totalTime: number; parallelism: number } }',
        outputSchema: '{ optimizations: { type: string; target: string; expectedSaving: number; cost: number; roi: number }[]; criticalPath: string[]; optimizedDuration: number; speedup: number }',
        keywords: ['优化', '并行', '批处理', '缓存', '关键路径'],
        relatedAlgorithms: ['ALG_T3_T_007', 'ALG_T3_D_010', 'ALG_T3_E_010'],
        documentationRef: '附录A·T3·时空·时间优化策略',
        useCases: ['性能优化', '效率提升', '流程改进', '资源优化'],
        limitations: ['优化有极限', '可能增加复杂度', '需权衡'],
        provenance: ['元构43条根本属性·D40', '时间优化理论'],
    },
    {
        id: 'ALG_T3_T_009',
        name: '时间感知机制',
        family: 'TIME',
        category: '时间感知',
        complexity: 'O(n)',
        description: '时间感知机制使系统能感知时间流逝和时序关系。感知包括：绝对时间（当前是何时）、相对时间（过了多久）、时序（先后顺序）、周期（重复模式）、节奏（速度变化）。感知支持时间敏感决策——根据时间调整行为。时间感知是智能系统的基础能力——理解时间才能做出合理的时间相关决策。',
        inputSchema: '{ events: { time: number; type: string }[]; context?: { timezone: string; businessHours: boolean; season: string } }',
        outputSchema: '{ perception: { currentTime: number; elapsed: number; sequence: string[]; periodicity: { period: number; confidence: number }[]; tempo: "slow" | "normal" | "fast" }; timeAwareDecisions: string[] }',
        keywords: ['感知', '时序', '周期', '节奏', '时间敏感'],
        relatedAlgorithms: ['ALG_T3_I_001', 'ALG_T3_T_002', 'ALG_T3_F_013'],
        documentationRef: '附录A·T3·时空·时间感知机制',
        useCases: ['时间敏感决策', '节奏管理', '周期识别', '时序理解'],
        limitations: ['感知需持续', '时区复杂', '主观时间差异'],
        provenance: ['元构43条根本属性·D40', '时间感知理论'],
    },
    {
        id: 'ALG_T3_T_010',
        name: '时间记忆机制',
        family: 'TIME',
        category: '时间记忆',
        complexity: 'O(n)',
        description: '时间记忆机制存储与时间相关的记忆——何时发生、持续多久、频率如何。时间记忆支持：时间标记（每条记忆有时间戳）、时间检索（按时间范围检索）、时间衰减（旧记忆衰减）、时间强化（频繁访问的记忆强化）。时间记忆是情景记忆的基础——记住"何时"发生的事。时间记忆与空间记忆共同构成情景记忆。',
        inputSchema: '{ memories: { content: unknown; timestamp: number; duration?: number; frequency?: number }[]; query?: { startTime: number; endTime: number } }',
        outputSchema: '{ stored: boolean; temporalIndex: { timestamp: number; memoryId: string; strength: number }[]; retrieved: { memory: unknown; timestamp: number; age: number; retention: number }[]; decayApplied: boolean }',
        keywords: ['记忆', '时间戳', '时间检索', '衰减'],
        relatedAlgorithms: ['ALG_T3_K_001', 'ALG_T3_K_010', 'ALG_T3_T_001'],
        documentationRef: '附录A·T3·时空·时间记忆机制',
        useCases: ['情景记忆', '时间检索', '历史回溯', '事件排序'],
        limitations: ['记忆可能丢失', '时间精度有限', '存储成本'],
        provenance: ['元构43条根本属性·D43', '时间记忆理论'],
    },
    {
        id: 'ALG_T3_T_011',
        name: '时间推理方法',
        family: 'TIME',
        category: '时间推理',
        complexity: 'O(n·m)',
        description: '时间推理方法基于时间信息进行推理——推断时间关系、预测未来、解释过去。推理支持：时序推理（A 在 B 之前还是之后）、持续时间推理（A 持续多久）、周期推理（A 多久重复一次）、因果时序（A 是否可能导致 B）。时间推理基于 Allen 时间区间代数——13 种基本时间关系。是事件理解和预测的基础。',
        inputSchema: '{ events: { id: string; startTime: number; endTime?: number; duration?: number }[]; query: string }',
        outputSchema: '{ relations: { a: string; b: string; relation: "before" | "after" | "during" | "overlaps" | "meets" | "equals"; confidence: number }[]; answer: unknown; causalChain?: { event: string; time: number; causes: string[] }[] }',
        keywords: ['推理', '时序', 'Allen', '区间代数', '因果时序'],
        relatedAlgorithms: ['ALG_T3_R_005', 'ALG_T3_R_010', 'ALG_T3_T_002'],
        documentationRef: '附录A·T3·时空·时间推理方法',
        useCases: ['事件理解', '因果推断', '时间预测', '调度优化'],
        limitations: ['时间信息可能不全', '模糊时间难推理', '并发事件复杂'],
        provenance: ['元构43条根本属性·D40', '时间推理理论'],
    },
    {
        id: 'ALG_T3_T_012',
        name: '时间冲突管理',
        family: 'TIME',
        category: '时间冲突',
        complexity: 'O(n²)',
        description: '时间冲突管理处理时间安排上的冲突——两个事件安排在同一时间、截止时间重叠、资源时间竞争。管理方法：时间重排（调整时间避免冲突）、优先级排序（高优先级优先）、并行处理（可以并行则并行）、时间扩展（延长截止时间）。时间冲突是最常见的冲突类型，需高效管理工具。',
        inputSchema: '{ events: { id: string; startTime: number; endTime: number; priority: number; flexible: boolean }[]; resources?: { id: string; capacity: number }[] }',
        outputSchema: '{ conflicts: { events: string[]; type: "overlap" | "deadline" | "resource"; severity: string }[]; resolution: { method: "reschedule" | "prioritize" | "parallelize" | "extend"; details: string }[]; schedule: { event: string; startTime: number; endTime: number }[] }',
        keywords: ['冲突', '重叠', '截止', '重排', '优先级'],
        relatedAlgorithms: ['ALG_T3_X_001', 'ALG_T3_T_007', 'ALG_T3_D_009'],
        documentationRef: '附录A·T3·时空·时间冲突管理',
        useCases: ['日程管理', '资源调度', '截止时间管理', '冲突解决'],
        limitations: ['某些时间不可调', '优先级主观', '并行有限制'],
        provenance: ['元构43条根本属性·D38', '时间冲突理论'],
    },
    {
        id: 'ALG_T3_T_013',
        name: '时间平衡策略',
        family: 'TIME',
        category: '时间平衡',
        complexity: 'O(n)',
        description: '时间平衡策略在多个时间需求之间寻找平衡——工作与生活、短期与长期、紧急与重要。平衡基于时间分配——将有限时间分配到不同需求。策略支持：时间块（固定时间块给固定活动）、弹性时间（预留弹性应对意外）、时间预算（按预算分配）、时间审计（定期审计使用）。平衡是时间管理的艺术——不是平均分配而是战略分配。',
        inputSchema: '{ demands: { category: string; required: number; importance: number; flexibility: number }[]; available: number; strategy?: "block" | "flexible" | "budget" | "audit" }',
        outputSchema: '{ allocation: { category: string; allocated: number; percentage: number; satisfaction: number }[]; balance: number; overspent: string[]; underspent: string[]; recommendations: string[] }',
        keywords: ['平衡', '工作生活', '短期长期', '紧急重要', '分配'],
        relatedAlgorithms: ['ALG_T3_T_007', 'ALG_T3_V_007', 'ALG_T3_D_009'],
        documentationRef: '附录A·T3·时空·时间平衡策略',
        useCases: ['时间管理', '工作生活平衡', '优先级管理', '效率优化'],
        limitations: ['平衡主观', '外部干扰', '需持续调整'],
        provenance: ['元构43条根本属性·D38', '时间平衡理论'],
    },
    {
        id: 'ALG_T3_T_014',
        name: '时间进化机制',
        family: 'TIME',
        category: '时间进化',
        complexity: 'O(n·m)',
        description: '时间进化机制描述系统对时间的理解和利用能力随时间提升的过程。进化分三阶段：被动响应（被时间驱动）、主动规划（主动规划时间）、时间超越（超越线性时间，多时间维度并行）。进化使系统能更好地利用时间——从时间奴隶变为时间主人。时间进化是元构时间智能的最高形态，支持多时间线推演和并行时间管理。',
        inputSchema: '{ history: { stage: string; capability: number; time: number }[]; currentStage: string; multiTimeline?: boolean }',
        outputSchema: '{ evolutionStage: "reactive" | "proactive" | "transcendent"; stageProgress: number; nextStageRequirements: string[]; multiTimelineCapability: number; timeUtilizationEfficiency: number }',
        keywords: ['进化', '被动', '主动', '超越', '多时间线'],
        relatedAlgorithms: ['ALG_T3_E_006', 'ALG_T3_T_009', 'ALG_T3_M_015'],
        documentationRef: '附录A·T3·时空·时间进化机制',
        useCases: ['时间能力提升', '多线管理', '时间智能', '系统进化'],
        limitations: ['进化缓慢', '可能退化', '需持续学习'],
        provenance: ['元构核心公理·A3', '时间进化理论'],
    },
];
function getConceptById(id) {
    return exports.CONCEPTS.find(c => c.id === id);
}
function searchConcepts(keywords) {
    const lowerKeywords = keywords.map(k => k.toLowerCase());
    return exports.CONCEPTS.filter(c => c.keywords.some(k => lowerKeywords.some(lk => k.toLowerCase().includes(lk))) ||
        lowerKeywords.some(lk => c.name.toLowerCase().includes(lk) || c.description.toLowerCase().includes(lk)));
}
