"use strict";
/**
 * MetaGO Engine - T3 概念存档 · 决策族（D 族）
 *
 * ALG_T3_D_001 ~ ALG_T3_D_013，共 13 个概念。
 * 覆盖决策锁、意图验证、意图谱系、语义输出门、内容完整性、回溯、评估、风险、权衡、收敛、分叉、记忆、进化。
 *
 * @author 易霄 / MetaGO Lightyear
 * @version 1.0.0
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.CONCEPTS = void 0;
exports.getConceptById = getConceptById;
exports.searchConcepts = searchConcepts;
exports.CONCEPTS = [
    {
        id: 'ALG_T3_D_001',
        name: '决策锁强制校验',
        family: 'DECISION',
        category: '决策锁',
        complexity: 'O(n)',
        description: '决策锁强制校验是元构核心保障机制——在 AI Agent 思考-行动链中设置强制校验节点，执行四道关卡：意图验证(IVL)、意图谱系追踪(ILT)、语义输出门(OSG)、内容完整性校验。每次输出前必须通过决策锁校验，禁止幻觉输出。决策锁是防止 AI 失控的最后防线，确保输出符合意图、可溯源、完整、无幻觉。',
        inputSchema: '{ intent: string; output: unknown; context: Context; lineage?: IntentLineage }',
        outputSchema: '{ passed: boolean; checks: { name: string; passed: boolean; details: string }[]; blockedReason?: string; recommendation: string }',
        keywords: ['决策锁', '校验', '意图', '语义', '完整性', '幻觉'],
        relatedAlgorithms: ['ALG_T3_D_002', 'ALG_T3_D_003', 'ALG_T3_D_004', 'ALG_T3_D_005'],
        documentationRef: '附录A·T3·决策·决策锁强制校验',
        useCases: ['AI 输出校验', '幻觉防护', '意图保证', '决策治理'],
        limitations: ['校验有延迟', '可能误拦', '需持续优化规则'],
        provenance: ['元构43条根本属性·D42合规主动', '决策锁理论'],
    },
    {
        id: 'ALG_T3_D_002',
        name: '意图验证机制',
        family: 'DECISION',
        category: '意图验证',
        complexity: 'O(n)',
        description: '意图验证机制（IVL）是决策锁的第一道关卡——验证输出是否符合用户原始意图。验证基于意图解析——将用户输入解析为意图向量，将输出解析为意图向量，计算对齐度。对齐度低于阈值则拦截。意图验证防止 AI 偏离用户意图——即使输出合理但非用户所需，也应拦截。',
        inputSchema: '{ userInput: string; output: unknown; intentVector?: number[]; threshold?: number }',
        outputSchema: '{ aligned: boolean; alignmentScore: number; intentVector: number[]; outputVector: number[]; deviation: string[] }',
        keywords: ['意图', '验证', 'IVL', '对齐'],
        relatedAlgorithms: ['ALG_T3_D_001', 'ALG_T3_V_002', 'ALG_T3_D_003'],
        documentationRef: '附录A·T3·决策·意图验证机制',
        useCases: ['意图保证', '偏离检测', '输出校验', '用户满意度'],
        limitations: ['意图解析困难', '阈值选择主观', '隐式意图难捕捉'],
        provenance: ['元构43条根本属性·D42', '意图验证理论'],
    },
    {
        id: 'ALG_T3_D_003',
        name: '意图谱系追踪',
        family: 'DECISION',
        category: '意图谱系',
        complexity: 'O(n)',
        description: '意图谱系追踪（ILT）是决策锁的第二道关卡——追踪意图从用户输入到最终输出的完整演化链路。谱系记录每一步的意图变化——为何变化、如何变化、变化是否合理。谱系追踪保证意图的可溯源性——任何输出都能回溯到原始意图。谱系是意图审计和意图争议解决的基础。',
        inputSchema: '{ rootIntent: string; transformations: { step: string; from: string; to: string; reason: string }[]; finalOutput: unknown }',
        outputSchema: '{ lineage: IntentLineage; traceable: boolean; deviations: { step: number; type: string; justified: boolean }[]; integrityScore: number }',
        keywords: ['谱系', '追踪', 'ILT', '溯源', '演化'],
        relatedAlgorithms: ['ALG_T3_D_001', 'ALG_T3_D_002', 'ALG_T3_K_001'],
        documentationRef: '附录A·T3·决策·意图谱系追踪',
        useCases: ['意图溯源', '审计', '争议解决', '意图演化分析'],
        limitations: ['谱系可能断裂', '隐式变化难记录', '记录成本'],
        provenance: ['元构核心公理·A1溯源公理', '意图谱系理论'],
    },
    {
        id: 'ALG_T3_D_004',
        name: '语义输出门',
        family: 'DECISION',
        category: '语义门',
        complexity: 'O(n)',
        description: '语义输出门（OSG）是决策锁的第三道关卡——检查输出语义是否符合规范。检查包括：禁用词检测（输出是否含禁用表述）、语义一致性（输出是否前后一致）、语义完整性（输出是否完整）、语义准确性（输出是否准确）。语义门防止语义层面的幻觉和不一致，是输出质量的语义级保障。',
        inputSchema: '{ output: string | object; rules: { forbiddenTerms?: string[]; requiredFields?: string[]; consistencyCheck?: boolean }[]; context?: Context }',
        outputSchema: '{ passed: boolean; violations: { type: string; location: string; detail: string; severity: string }[]; warnings: string[]; semanticScore: number }',
        keywords: ['语义', '门', 'OSG', '禁用词', '一致性'],
        relatedAlgorithms: ['ALG_T3_D_001', 'ALG_T3_D_005', 'ALG_T3_S_001'],
        documentationRef: '附录A·T3·决策·语义输出门',
        useCases: ['输出质量保障', '禁用词过滤', '语义一致性', '幻觉防护'],
        limitations: ['规则需维护', '语义理解有限', '可能误报'],
        provenance: ['元构43条根本属性·D42', '语义输出门理论'],
    },
    {
        id: 'ALG_T3_D_005',
        name: '内容完整性校验',
        family: 'DECISION',
        category: '完整性',
        complexity: 'O(n)',
        description: '内容完整性校验是决策锁的第四道关卡——检查输出内容是否完整，无遗漏、无占位符、无幻觉。校验包括：占位符检测（是否有 {placeholder} 未替换）、幻觉检测（是否有虚构的 API/数据/引用）、缺失检测（是否有应包含但缺失的内容）、重复检测（是否有冗余重复）。完整性校验是输出可靠性的最后保障。',
        inputSchema: '{ output: unknown; expectedElements?: string[]; referenceData?: unknown[]; placeholderPatterns?: string[] }',
        outputSchema: '{ complete: boolean; issues: { type: "placeholder" | "hallucination" | "missing" | "duplicate"; location: string; detail: string; severity: string }[]; completenessScore: number }',
        keywords: ['完整性', '占位符', '幻觉', '缺失', '重复'],
        relatedAlgorithms: ['ALG_T3_D_001', 'ALG_T3_D_004', 'ALG_T3_M_006'],
        documentationRef: '附录A·T3·决策·内容完整性校验',
        useCases: ['输出可靠性', '占位符防护', '幻觉检测', '质量保证'],
        limitations: ['检测规则有限', '新型幻觉难检测', '需参考数据'],
        provenance: ['元构核心公理·A2闭环公理', '内容完整性理论'],
    },
    {
        id: 'ALG_T3_D_006',
        name: '决策回溯机制',
        family: 'DECISION',
        category: '决策回溯',
        complexity: 'O(n)',
        description: '决策回溯机制在决策失败或产生负面效果时，回退到决策点重新选择。回溯基于决策树——记录每个决策点的选项和选择，失败时回到分叉点尝试其他选项。回溯支持完全回溯（回到根决策点）和部分回溯（回到最近分叉点）。回溯是决策学习的核心——从失败中学习，避免重复错误。',
        inputSchema: '{ decisionTree: DecisionNode[]; failurePoint: { nodeId: string; error: string }; mode?: "full" | "partial" }',
        outputSchema: '{ rolledBack: boolean; restoredNode: DecisionNode; alternativePaths: { nodeId: string; option: string; estimatedOutcome: string }[]; learnedLessons: string[] }',
        keywords: ['回溯', '回退', '决策树', '学习'],
        relatedAlgorithms: ['ALG_T3_E_011', 'ALG_T3_D_001', 'ALG_T3_K_013'],
        documentationRef: '附录A·T3·决策·决策回溯机制',
        useCases: ['决策失败恢复', '替代方案探索', '错误学习', '决策优化'],
        limitations: ['回溯成本高', '可能丢失进度', '需完整决策记录'],
        provenance: ['元构核心公理·A2', '决策回溯理论'],
    },
    {
        id: 'ALG_T3_D_007',
        name: '决策评估方法',
        family: 'DECISION',
        category: '决策评估',
        complexity: 'O(n)',
        description: '决策评估方法从多维度评价决策质量。维度包括：逻辑完整性（推理是否严密）、证据充分性（是否有足够证据）、风险可控性（风险是否可控）、替代方案考虑（是否考虑其他方案）、可执行性（能否落地）、时效性（时机是否合适）。评估支持事前评估（决策前）和事后评估（决策后）。',
        inputSchema: '{ decision: { options: Option[]; selected: string; reasoning: string; evidence: Evidence[] }; stage: "pre" | "post"; outcome?: unknown }',
        outputSchema: '{ scores: { dimension: string; score: number; rationale: string }[]; overallScore: number; grade: string; risks: Risk[]; improvements: string[] }',
        keywords: ['评估', '逻辑', '证据', '风险', '可执行'],
        relatedAlgorithms: ['ALG_T1_V_001', 'ALG_T3_D_001', 'ALG_T3_D_008'],
        documentationRef: '附录A·T3·决策·决策评估方法',
        useCases: ['决策质量保证', '决策改进', '风险评估', '决策审查'],
        limitations: ['维度选择主观', '事后评估有偏', '需充分信息'],
        provenance: ['元构43条根本属性·D38', '决策评估理论'],
    },
    {
        id: 'ALG_T3_D_008',
        name: '决策风险分析',
        family: 'DECISION',
        category: '决策风险',
        complexity: 'O(n)',
        description: '决策风险分析识别、量化、评估决策可能带来的风险。风险分类型：执行风险（能否落地）、结果风险（结果是否如预期）、副作用风险（是否有意外后果）、 reversibility 风险（是否可逆）。分析输出风险矩阵——风险 × 概率 × 影响，指导风险缓解策略。高风险决策需决策锁强化校验。',
        inputSchema: '{ decision: Decision; context: Context; riskAppetite?: "low" | "medium" | "high" }',
        outputSchema: '{ risks: { type: string; description: string; probability: number; impact: number; severity: number; mitigation: string }[]; overallRisk: number; riskLevel: "low" | "medium" | "high" | "critical"; recommendations: string[] }',
        keywords: ['风险', '概率', '影响', '缓解'],
        relatedAlgorithms: ['ALG_T1_V_016', 'ALG_T3_D_007', 'ALG_T3_S_001'],
        documentationRef: '附录A·T3·决策·决策风险分析',
        useCases: ['风险评估', '决策保障', '风险缓解', '决策审查'],
        limitations: ['风险预测不确定', '黑天鹅事件', '需充分信息'],
        provenance: ['元构43条根本属性·D42', '决策风险理论'],
    },
    {
        id: 'ALG_T3_D_009',
        name: '决策权衡方法',
        family: 'DECISION',
        category: '决策权衡',
        complexity: 'O(n²)',
        description: '决策权衡方法在多个冲突目标之间寻找最优平衡。权衡基于多目标优化——帕累托前沿识别非劣解，加权聚合选择最优解。权衡支持不同策略：保守（最小化最大损失）、进取（最大化最大收益）、均衡（最大化期望值）。权衡是复杂决策的核心——很少有完美决策，只有最优权衡。',
        inputSchema: '{ objectives: { name: string; direction: "max" | "min"; weight: number; currentValue: number }[]; options: Option[]; strategy?: "conservative" | "aggressive" | "balanced" }',
        outputSchema: '{ paretoFront: Option[]; selected: Option; tradeoffs: { objective: string; sacrifice: number; gain: number }[]; justification: string }',
        keywords: ['权衡', '帕累托', '多目标', '优化'],
        relatedAlgorithms: ['ALG_T1_V_007', 'ALG_T3_V_003', 'ALG_T3_X_001'],
        documentationRef: '附录A·T3·决策·决策权衡方法',
        useCases: ['多目标决策', '资源分配', '方案选择', '冲突解决'],
        limitations: ['帕累托前沿计算复杂', '权重主观', '维度灾难'],
        provenance: ['元构43条根本属性·D38', '决策权衡理论'],
    },
    {
        id: 'ALG_T3_D_010',
        name: '决策收敛机制',
        family: 'DECISION',
        category: '决策收敛',
        complexity: 'O(n log n)',
        description: '决策收敛机制将多个候选决策逐步聚焦到最终决策。收敛通过筛选（按约束排除不合规）、评估（按目标排序）、细化（深入发展 Top-N）、选择（最终决策）四步。收敛平衡速度和质量——快速收敛可能漏掉好选项，慢速收敛可能错过时机。收敛支持中止条件（找到足够好的就停）和穷尽条件（评估完所有）。',
        inputSchema: '{ candidates: Decision[]; constraints: Constraint[]; objectives: Objective[]; convergenceStrategy?: "fast" | "thorough" | "satisficing" }',
        outputSchema: '{ converged: Decision; path: { stage: string; candidatesRemaining: number; criteria: string }[]; abandoned: Decision[]; convergenceTime: number }',
        keywords: ['收敛', '筛选', '评估', '细化', '选择'],
        relatedAlgorithms: ['ALG_T3_M_010', 'ALG_T3_D_009', 'ALG_T3_D_007'],
        documentationRef: '附录A·T3·决策·决策收敛机制',
        useCases: ['方案选择', '决策聚焦', '资源优化', '时机把握'],
        limitations: ['收敛速度权衡', '可能漏掉好选项', '主观判断'],
        provenance: ['元构43条根本属性·D40', '决策收敛理论'],
    },
    {
        id: 'ALG_T3_D_011',
        name: '决策分叉管理',
        family: 'DECISION',
        category: '决策分叉',
        complexity: 'O(n)',
        description: '决策分叉管理在决策有多个可能路径时，并行探索多个路径。分叉基于不确定性——当无法确定哪个路径最优时，并行探索多个。分叉支持资源分配（每条路径分配多少资源）、终止条件（何时放弃某条路径）、合并策略（多条路径结果如何合并）。分叉增加决策鲁棒性，但消耗更多资源。',
        inputSchema: '{ decisionPoint: Decision; branches: { path: string; probability: number; estimatedValue: number; cost: number }[]; resourceBudget: number }',
        outputSchema: '{ allocation: { path: string; resources: number; expectedValue: number }[]; terminationCriteria: { path: string; condition: string }[]; mergeStrategy: string; totalExpectedValue: number }',
        keywords: ['分叉', '并行', '探索', '合并'],
        relatedAlgorithms: ['ALG_T3_E_013', 'ALG_T3_D_010', 'ALG_T3_D_009'],
        documentationRef: '附录A·T3·决策·决策分叉管理',
        useCases: ['不确定决策', '多方案探索', '风险分散', '决策鲁棒性'],
        limitations: ['资源消耗大', '合并复杂', '可能浪费'],
        provenance: ['元构43条根本属性·D40', '决策分叉理论'],
    },
    {
        id: 'ALG_T3_D_012',
        name: '决策记忆机制',
        family: 'DECISION',
        category: '决策记忆',
        complexity: 'O(n)',
        description: '决策记忆机制将历史决策及其结果存储为可复用的决策记忆。记忆分成功决策（什么有效）、失败决策（什么无效）、决策模式（如何决策）三类。记忆支持检索（按场景找类似决策）和迁移（将决策模式应用到新场景）。决策记忆使决策能力可积累、可复用、可进化，避免重复犯错。',
        inputSchema: '{ decision: Decision; outcome: "success" | "failure"; context: Context; patterns?: DecisionPattern[] }',
        outputSchema: '{ stored: boolean; memoryType: "success" | "failure" | "pattern"; retrievalKeys: string[]; transferableTo: string[]; memoryStrength: number }',
        keywords: ['记忆', '决策', '复用', '迁移', '积累'],
        relatedAlgorithms: ['ALG_T3_K_001', 'ALG_T3_K_013', 'ALG_T3_M_014'],
        documentationRef: '附录A·T3·决策·决策记忆机制',
        useCases: ['决策经验积累', '模式复用', '决策迁移', '避免重复错误'],
        limitations: ['记忆可能过时', '场景差异', '需适配'],
        provenance: ['元构43条根本属性·D43', '决策记忆理论'],
    },
    {
        id: 'ALG_T3_D_013',
        name: '决策进化机制',
        family: 'DECISION',
        category: '决策进化',
        complexity: 'O(n·m)',
        description: '决策进化机制将决策能力本身作为进化对象——从每次决策中学习，改进决策方法。进化基于反馈循环——决策→结果→反馈→改进→再决策。进化支持元决策——决策"如何更好决策"的方法。决策进化使决策能力持续提升，从经验决策到模式决策到元决策。是决策能力的最高形态。',
        inputSchema: '{ decisionHistory: { decision: Decision; outcome: string; feedback: Feedback; lesson: string }[]; currentMethod: DecisionMethod }',
        outputSchema: '{ evolvedMethod: DecisionMethod; improvementGain: number; evolutionStage: "experience" | "pattern" | "meta"; emergentInsights: string[]; nextEvolutionFocus: string }',
        keywords: ['进化', '反馈', '元决策', '学习'],
        relatedAlgorithms: ['ALG_T3_E_006', 'ALG_T3_D_012', 'ALG_T3_M_015'],
        documentationRef: '附录A·T3·决策·决策进化机制',
        useCases: ['决策能力提升', '持续改进', '元决策', '决策学习'],
        limitations: ['进化缓慢', '可能退化', '需充分反馈'],
        provenance: ['元构核心公理·A3', '决策进化理论'],
    },
];
function getConceptById(id) {
    return exports.CONCEPTS.find(c => c.id === id);
}
function searchConcepts(keywords) {
    const lowerKeywords = keywords.map(k => k.toLowerCase());
    return exports.CONCEPTS.filter(c => c.keywords.some(k => lowerKeywords.some(lk => k.toLowerCase().includes(lk))) ||
        lowerKeywords.some(lk => c.name.toLowerCase().includes(lk) || c.description.toLowerCase().includes(lk)));
}
