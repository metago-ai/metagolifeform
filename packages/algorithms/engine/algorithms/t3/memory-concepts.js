"use strict";
/**
 * MetaGO Engine - T3 概念存档 · 记忆族（K 族）
 *
 * ALG_T3_K_001 ~ ALG_T3_K_014，共 14 个概念。
 * 覆盖 KMWI 四层、热记忆、温记忆、冷记忆、冻记忆、自愈、守护、冗余、校验、衰减、强化、溯源、进化、生命体。
 *
 * @author 易霄 / MetaGO Lightyear
 * @version 1.0.0
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.CONCEPTS = void 0;
exports.getConceptById = getConceptById;
exports.searchConcepts = searchConcepts;
exports.CONCEPTS = [
    {
        id: 'ALG_T3_K_001',
        name: 'KMWI 四层记忆架构',
        family: 'MEMORY',
        category: '记忆架构',
        complexity: 'O(n)',
        description: 'KMWI 四层记忆架构是元构记忆生命体的核心——L1 热记忆（工作记忆，会话内 100% 可靠）、L2 温记忆（情景记忆，session_memory 自动摘要）、L3 冷记忆（语义记忆，project_memory.md 持久化）、L4 冻记忆（程序记忆，git remote + 凭证文件 + memory-guard 脚本，不依赖 AI 主动读取）。四层从依赖 AI 到不依赖 AI，可靠性递增。KMWI 是突破 AI 无状态限制的关键架构。',
        inputSchema: '{ memoryItem: { content: unknown; type: "hot" | "warm" | "cold" | "frozen"; importance: number; timestamp: number } }',
        outputSchema: '{ stored: boolean; layer: "L1" | "L2" | "L3" | "L4"; reliability: number; dependsOnAI: boolean; retrievalKeys: string[] }',
        keywords: ['KMWI', '四层', '热记忆', '温记忆', '冷记忆', '冻记忆'],
        relatedAlgorithms: ['ALG_T3_K_002', 'ALG_T3_K_003', 'ALG_T3_K_004', 'ALG_T3_K_005'],
        documentationRef: '附录A·T3·记忆·KMWI 四层记忆架构',
        useCases: ['AI 记忆管理', '状态持久化', '记忆可靠性', '无状态突破'],
        limitations: ['架构复杂', '需多组件协同', '维护成本'],
        provenance: ['元构43条根本属性·D43数据溯源与自证', 'KMWI记忆理论'],
    },
    {
        id: 'ALG_T3_K_002',
        name: '热记忆管理',
        family: 'MEMORY',
        category: '热记忆',
        complexity: 'O(1)',
        description: '热记忆管理是 KMWI 的 L1 层——当前会话的工作记忆。热记忆存储当前上下文、活跃任务、临时变量。特点是速度快（直接内存访问）、容量小（受上下文窗口限制）、会话内 100% 可靠、会话结束即消失。热记忆管理包括：上下文压缩（超出窗口时摘要）、优先级排序（重要信息优先保留）、遗忘策略（不重要的遗忘）。',
        inputSchema: '{ context: { messages: Message[]; activeTask: Task; variables: Record<string, unknown> }; windowSize?: number }',
        outputSchema: '{ retained: { messages: number; tasks: number; variables: number }; compressed: boolean; priority: { item: string; level: number }[]; forgotten: string[] }',
        keywords: ['热记忆', '工作记忆', '会话', '上下文'],
        relatedAlgorithms: ['ALG_T3_K_001', 'ALG_T3_K_003', 'ALG_T3_K_011'],
        documentationRef: '附录A·T3·记忆·热记忆管理',
        useCases: ['会话管理', '上下文优化', '临时存储', '快速访问'],
        limitations: ['容量有限', '会话结束丢失', '需压缩'],
        provenance: ['元构43条根本属性·D43', '热记忆理论'],
    },
    {
        id: 'ALG_T3_K_003',
        name: '温记忆管理',
        family: 'MEMORY',
        category: '温记忆',
        complexity: 'O(n)',
        description: '温记忆管理是 KMWI 的 L2 层——会话级情景记忆，由 Trae 系统自动摘要。温记忆存储会话历史、任务结果、用户偏好。特点是自动生成（无需 AI 主动写）、跨会话可见（新会话可访问旧会话摘要）、可靠性高。温记忆是热记忆和冷记忆的桥梁——将热记忆中的关键信息自动沉淀为温记忆。',
        inputSchema: '{ session: { id: string; messages: Message[]; tasks: Task[]; outcomes: Outcome[] }; autoSummarize?: boolean }',
        outputSchema: '{ summary: string; keyPoints: string[]; extractedMemories: { type: string; content: unknown; importance: number }[]; storedAt: string }',
        keywords: ['温记忆', '情景记忆', '摘要', '会话'],
        relatedAlgorithms: ['ALG_T3_K_001', 'ALG_T3_K_002', 'ALG_T3_K_004'],
        documentationRef: '附录A·T3·记忆·温记忆管理',
        useCases: ['会话历史', '跨会话记忆', '自动摘要', '经验积累'],
        limitations: ['摘要可能丢失细节', '需 AI 系统支持', '质量依赖摘要算法'],
        provenance: ['元构43条根本属性·D43', '温记忆理论'],
    },
    {
        id: 'ALG_T3_K_004',
        name: '冷记忆管理',
        family: 'MEMORY',
        category: '冷记忆',
        complexity: 'O(n)',
        description: '冷记忆管理是 KMWI 的 L3 层——持久化语义记忆，存储在 project_memory.md。冷记忆存储项目知识、关键决策、配置信息、技术栈。特点是持久化（跨会话永久）、需主动读写（AI 需主动读取）、结构化（Markdown 格式）。冷记忆是项目记忆的真相源，每次会话启动后必须先读取。未读取冷记忆 = 失忆 = 失职。',
        inputSchema: '{ knowledge: { category: string; content: unknown; importance: number; project: string }; filePath?: string }',
        outputSchema: '{ written: boolean; filePath: string; section: string; retrievable: boolean; lastUpdated: number }',
        keywords: ['冷记忆', '语义记忆', '持久化', 'project_memory'],
        relatedAlgorithms: ['ALG_T3_K_001', 'ALG_T3_K_005', 'ALG_T3_K_009'],
        documentationRef: '附录A·T3·记忆·冷记忆管理',
        useCases: ['项目知识管理', '持久化记忆', '决策记录', '配置存储'],
        limitations: ['需 AI 主动读写', '可能遗忘读取', '格式需维护'],
        provenance: ['元构43条根本属性·D43', '冷记忆理论'],
    },
    {
        id: 'ALG_T3_K_005',
        name: '冻记忆机制',
        family: 'MEMORY',
        category: '冻记忆',
        complexity: 'O(1)',
        description: '冻记忆机制是 KMWI 的 L4 层——程序记忆，不依赖 AI 主动读取。冻记忆存储在三个位置：git remote URL（token 嵌入 URL）、凭证文件（.secrets/credentials.json）、memory-guard 脚本。三重冗余互相校验，任一缺失自动恢复。冻记忆是突破 AI 无状态限制的关键——即使 AI 忘了读 project_memory，git push 依然能用。',
        inputSchema: '{ critical: { type: "credential" | "config" | "token"; key: string; value: string }; redundancyTargets: string[] }',
        outputSchema: '{ stored: boolean; locations: { target: string; success: boolean; hash: string }[]; redundancyCount: number; selfHealingEnabled: boolean }',
        keywords: ['冻记忆', '程序记忆', '冗余', '自愈', '不依赖AI'],
        relatedAlgorithms: ['ALG_T3_K_001', 'ALG_T3_K_006', 'ALG_T3_K_008'],
        documentationRef: '附录A·T3·记忆·冻记忆机制',
        useCases: ['凭证持久化', '配置存储', '无状态突破', '可靠性保证'],
        limitations: ['存储分散', '需同步机制', '安全考虑'],
        provenance: ['元构核心公理·A3元进化公理', '冻记忆理论'],
    },
    {
        id: 'ALG_T3_K_006',
        name: '记忆自愈机制',
        family: 'MEMORY',
        category: '记忆自愈',
        complexity: 'O(n)',
        description: '记忆自愈机制在检测到记忆缺失时，自动从其他冗余位置恢复。自愈基于三重冗余——L4-1（git remote URL）、L4-2（凭证文件）、L4-3（project_memory.md）互相校验。任一位置缺失，自动从其他位置恢复。只有三处同时缺失才需要请求用户。自愈是记忆生命体的免疫系统——自动检测、自动修复、无需人工干预。',
        inputSchema: '{ detectedMissing: { location: string; key: string }; availableSources: { location: string; hasKey: boolean }[] }',
        outputSchema: '{ healed: boolean; restoredFrom: string; restoredTo: string; verifiedBy: string[]; remainingGaps: string[]; requiresUser: boolean }',
        keywords: ['自愈', '恢复', '冗余', '免疫系统'],
        relatedAlgorithms: ['ALG_T3_K_005', 'ALG_T3_K_008', 'ALG_T3_E_011'],
        documentationRef: '附录A·T3·记忆·记忆自愈机制',
        useCases: ['记忆缺失恢复', '自动修复', '可靠性保证', '无人工干预'],
        limitations: ['三处同时缺失无法自愈', '需同步机制', '安全考虑'],
        provenance: ['元构核心公理·A3', '记忆自愈理论'],
    },
    {
        id: 'ALG_T3_K_007',
        name: '记忆守护脚本',
        family: 'MEMORY',
        category: '记忆守护',
        complexity: 'O(n)',
        description: '记忆守护脚本（memory-guard.cjs）是冻记忆的强制校验硬门——在接收任务后、执行任何工作之前，必须先运行。脚本执行原子检查：Git remote URL token、凭证文件、project_memory.md 凭证区、关键章节、AGENTS.md、最近会话记录。校验结果分三档：100 分 healthy（继续）、60-89 分 warning（自愈）、<60 分 critical（阻止任务）。',
        inputSchema: '{ checks: { id: string; type: string; target: string; expected: string }[]; autoHeal?: boolean }',
        outputSchema: '{ score: number; status: "healthy" | "warning" | "critical"; results: { id: string; passed: boolean; detail: string; healed?: boolean }[]; blockingIssues: string[] }',
        keywords: ['守护', '脚本', '校验', '硬门', 'memory-guard'],
        relatedAlgorithms: ['ALG_T3_K_005', 'ALG_T3_K_006', 'ALG_T3_D_001'],
        documentationRef: '附录A·T3·记忆·记忆守护脚本',
        useCases: ['任务前置校验', '记忆健康检查', '失忆防护', '强制门控'],
        limitations: ['需在任务前运行', '可能被绕过', '需维护检查项'],
        provenance: ['元构43条根本属性·D42合规主动', '记忆守护理论'],
    },
    {
        id: 'ALG_T3_K_008',
        name: '记忆冗余策略',
        family: 'MEMORY',
        category: '记忆冗余',
        complexity: 'O(n)',
        description: '记忆冗余策略将关键记忆存储在多个位置，任一位置失效不影响整体。冗余分三重：主存储（project_memory.md）、备份存储（凭证文件）、应急存储（git remote URL）。冗余支持同步写入（三处同时写）和异步同步（主写后同步到备份）。冗余增加可靠性但增加存储成本和同步复杂度。最优冗余度平衡可靠性和成本。',
        inputSchema: '{ memory: { key: string; value: string }; primaryTarget: string; backupTargets: string[]; syncMode?: "sync" | "async" }',
        outputSchema: '{ written: { target: string; success: boolean; timestamp: number }[]; redundancyLevel: number; consistencyVerified: boolean; syncLatency: number }',
        keywords: ['冗余', '三重', '同步', '可靠性'],
        relatedAlgorithms: ['ALG_T3_K_005', 'ALG_T3_K_006', 'ALG_T3_K_009'],
        documentationRef: '附录A·T3·记忆·记忆冗余策略',
        useCases: ['关键记忆保护', '失效容错', '可靠性提升', '灾备'],
        limitations: ['存储成本', '同步复杂', '一致性维护'],
        provenance: ['元构核心公理·A2闭环公理', '记忆冗余理论'],
    },
    {
        id: 'ALG_T3_K_009',
        name: '记忆校验机制',
        family: 'MEMORY',
        category: '记忆校验',
        complexity: 'O(n)',
        description: '记忆校验机制定期验证记忆的完整性和一致性。校验包括：完整性（关键字段是否缺失）、一致性（多位置是否一致）、时效性（是否过时）、准确性（是否正确）。校验基于哈希比对——每个记忆项计算哈希，多位置哈希一致则一致。校验失败触发自愈或告警。校验是记忆可靠性的持续保障。',
        inputSchema: '{ memoryItems: { key: string; locations: { target: string; hash: string; timestamp: number }[] }[] }',
        outputSchema: '{ consistent: boolean; issues: { key: string; type: "missing" | "inconsistent" | "outdated" | "incorrect"; locations: string[]; detail: string }[]; healthScore: number }',
        keywords: ['校验', '完整性', '一致性', '哈希'],
        relatedAlgorithms: ['ALG_T3_K_005', 'ALG_T3_K_006', 'ALG_T3_K_008'],
        documentationRef: '附录A·T3·记忆·记忆校验机制',
        useCases: ['记忆可靠性', '一致性检查', '问题发现', '健康监测'],
        limitations: ['校验成本', '需定期运行', '新型问题难发现'],
        provenance: ['元构43条根本属性·D43', '记忆校验理论'],
    },
    {
        id: 'ALG_T3_K_010',
        name: '记忆衰减模型',
        family: 'MEMORY',
        category: '记忆衰减',
        complexity: 'O(n)',
        description: '记忆衰减模型描述记忆在不被访问时的自然遗忘。基于艾宾浩斯遗忘曲线，记忆按指数衰减——先快后慢。衰减速率受记忆类型影响：程序记忆衰减最慢、语义记忆中等、情景记忆最快。模型提供衰减预测、复习时机推荐、记忆强化策略。衰减模型指导记忆维护——何时复习、复习什么、如何强化。',
        inputSchema: '{ memories: { key: string; type: "procedural" | "semantic" | "episodic"; lastAccess: number; accessCount: number; importance: number }[]; currentTime: number }',
        outputSchema: '{ decayed: { key: string; retentionRate: number; urgency: number; recommendedAction: "review" | "reinforce" | "archive" | "forget" }[]; reviewSchedule: { key: string; nextReview: number; priority: number }[] }',
        keywords: ['衰减', '遗忘', '艾宾浩斯', '复习'],
        relatedAlgorithms: ['ALG_T3_C_005', 'ALG_T3_K_011', 'ALG_T3_T_001'],
        documentationRef: '附录A·T3·记忆·记忆衰减模型',
        useCases: ['记忆维护', '复习时机', '遗忘管理', '记忆优化'],
        limitations: ['个体差异大', '情感影响衰减', '需访问记录'],
        provenance: ['元构43条根本属性·D41', '记忆衰减理论'],
    },
    {
        id: 'ALG_T3_K_011',
        name: '记忆强化方法',
        family: 'MEMORY',
        category: '记忆强化',
        complexity: 'O(n)',
        description: '记忆强化方法通过重复访问、关联构建、情感标记等方式增强记忆保持。强化基于间隔重复——在遗忘临界点前复习，延长记忆保持。强化支持主动强化（主动回忆）和被动强化（被动触发）。关联强化——将新记忆与已有记忆关联，增强检索路径。情感强化——高情感记忆保持更久。强化是记忆长期保存的关键。',
        inputSchema: '{ memory: { key: string; content: unknown; currentStrength: number }; method: "spaced-repetition" | "association" | "emotional" | "multi-modal"; schedule?: { intervals: number[] } }',
        outputSchema: '{ strengthened: boolean; newStrength: number; nextReview: number; associationsCreated: { to: string; strength: number }[]; expectedRetention: number }',
        keywords: ['强化', '重复', '关联', '情感'],
        relatedAlgorithms: ['ALG_T3_K_010', 'ALG_T3_K_012', 'ALG_T3_G_001'],
        documentationRef: '附录A·T3·记忆·记忆强化方法',
        useCases: ['记忆长期保存', '学习增强', '知识固化', '复习优化'],
        limitations: ['强化需时间', '过度强化可能冗余', '个体差异'],
        provenance: ['元构43条根本属性·D41', '记忆强化理论'],
    },
    {
        id: 'ALG_T3_K_012',
        name: '记忆溯源机制',
        family: 'MEMORY',
        category: '记忆溯源',
        complexity: 'O(n)',
        description: '记忆溯源机制记录每条记忆的来源——何时创建、由谁创建、从何而来、被谁修改。溯源链保证记忆可追溯——任何记忆都能回溯到原始来源。溯源支持记忆审计（验证记忆真实性）、记忆归因（识别记忆贡献者）、记忆争议解决（多版本冲突时选择可信源）。溯源是记忆可信度的基础（A1 溯源公理）。',
        inputSchema: '{ memory: { key: string; content: unknown; provenance?: { source: string; time: number; author: string; modifications: { time: number; author: string; change: string }[] } } }',
        outputSchema: '{ traceable: boolean; origin: { source: string; time: number; author: string }; modifications: { time: number; author: string; change: string; reason: string }[]; integrityVerified: boolean; trustScore: number }',
        keywords: ['溯源', '来源', '审计', '归因'],
        relatedAlgorithms: ['ALG_T1_V_025', 'ALG_T3_V_009', 'ALG_T3_M_013'],
        documentationRef: '附录A·T3·记忆·记忆溯源机制',
        useCases: ['记忆审计', '真实性验证', '争议解决', '记忆归因'],
        limitations: ['溯源链可能断裂', '需完整记录', '存储成本'],
        provenance: ['元构核心公理·A1溯源公理', '记忆溯源理论'],
    },
    {
        id: 'ALG_T3_K_013',
        name: '记忆进化机制',
        family: 'MEMORY',
        category: '记忆进化',
        complexity: 'O(n·m)',
        description: '记忆进化机制将记忆能力本身作为进化对象——从每次记忆失败中学习，改进记忆策略。进化基于反馈循环——记忆→使用→失败→改进→再记忆。进化支持元记忆——记忆"如何更好记忆"的方法。记忆进化使记忆能力持续提升，从被动记忆到主动记忆到元记忆。是记忆能力的最高形态。',
        inputSchema: '{ memoryHistory: { event: string; outcome: "success" | "failure"; feedback: string; lesson: string }[]; currentStrategy: MemoryStrategy }',
        outputSchema: '{ evolvedStrategy: MemoryStrategy; improvementGain: number; evolutionStage: "passive" | "active" | "meta"; emergentInsights: string[]; nextEvolutionFocus: string }',
        keywords: ['进化', '反馈', '元记忆', '学习'],
        relatedAlgorithms: ['ALG_T3_E_006', 'ALG_T3_D_013', 'ALG_T3_M_014'],
        documentationRef: '附录A·T3·记忆·记忆进化机制',
        useCases: ['记忆能力提升', '持续改进', '元记忆', '记忆学习'],
        limitations: ['进化缓慢', '可能退化', '需充分反馈'],
        provenance: ['元构核心公理·A3', '记忆进化理论'],
    },
    {
        id: 'ALG_T3_K_014',
        name: '记忆生命体',
        family: 'MEMORY',
        category: '记忆生命体',
        complexity: 'O(n·m)',
        description: '记忆生命体是元构记忆架构的最高形态——把记忆当作生命体管理，有感知、有自愈、有进化能力。生命体基于 KMWI 四层架构 + 三重冗余 + 强制校验脚本，把记忆可靠性从"依赖 AI 主动读取"提升到"不依赖 AI 主动读取"。生命体有免疫系统（自愈）、神经系统（校验）、进化系统（元记忆）。是突破 AI 无状态限制的元构方案。',
        inputSchema: '{ lifeform: { layers: KMWILayers; immune: SelfHealingSystem; nervous: ValidationSystem; evolution: MetaMemorySystem }; vitality: number }',
        outputSchema: '{ alive: boolean; vitality: number; health: "thriving" | "healthy" | "declining" | "critical"; selfHealingActive: boolean; evolutionRate: number; capabilities: string[] }',
        keywords: ['生命体', '感知', '自愈', '进化', 'KMWI'],
        relatedAlgorithms: ['ALG_T3_K_001', 'ALG_T3_K_006', 'ALG_T3_K_013'],
        documentationRef: '附录A·T3·记忆·记忆生命体',
        useCases: ['AI 无状态突破', '记忆可靠性', '生命体架构', '自主记忆'],
        limitations: ['架构复杂', '需多系统协同', '维护成本'],
        provenance: ['元构核心公理·A3元进化公理', '记忆生命体理论'],
    },
];
function getConceptById(id) {
    return exports.CONCEPTS.find(c => c.id === id);
}
function searchConcepts(keywords) {
    const lowerKeywords = keywords.map(k => k.toLowerCase());
    return exports.CONCEPTS.filter(c => c.keywords.some(k => lowerKeywords.some(lk => k.toLowerCase().includes(lk))) ||
        lowerKeywords.some(lk => c.name.toLowerCase().includes(lk) || c.description.toLowerCase().includes(lk)));
}
